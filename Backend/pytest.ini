[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .

[pytest-asyncio]
; All async tests and fixtures share a single event loop for the
; entire session — required because the SQLAlchemy engine and Redis
; client are module-level singletons bound to one loop.
loop_scope = session
