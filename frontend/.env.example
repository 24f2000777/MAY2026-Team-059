# Base URL of the FastAPI backend. Defaults to http://localhost:8000
# if this isn't set at all, so this file is only needed if the backend
# runs somewhere else (a different port, a deployed URL, etc).
VITE_API_URL=http://localhost:8000
